# 周易音乐播放器 - 开发文档

## 整体结构

这个项目分为三层：界面层、业务层、底层库。
界面层用 QML 编写，负责所有用户能看到的东西——窗口、按钮、播放列表、歌词显示。业务层用 C++ 编写，负责真正的逻辑——播放音乐、管理列表、保存数据。它们之间通过 Qt 的信号和属性机制互相通信。

## 核心模块说明

### 播放管理器 (MusicManager)

这是整个程序的大脑，控制所有和播放相关的操作。
它内置了一个 QMediaPlayer（Qt 提供的音乐播放器），把播放、暂停、切歌、调音量这些操作包装成 QML 可以直接调用的方法。比如 QML 里写 `musicManager.play()` 就会开始播放，写 `musicManager.next()` 就会切到下一首。
播放模式有三种，按顺序切换：顺序播放 → 随机播放 → 单曲循环 → 回到顺序播放。
歌词功能也在这里处理。当播放一首歌时，它会自动去歌曲所在目录找同名的 `.lrc` 歌词文件，解析里面的时间标签和歌词文本，然后在播放过程中根据当前进度，把当前应该唱到哪一行告诉 QML 界面。
程序会把这些时间戳和文字提取出来，按时间排序，存成一个列表。播放时根据当前进度毫秒数，找到对应的那一行，发信号通知 QML 高亮显示。
### 播放列表 (PlaylistModel)

这个类管理所有歌曲的数据。它继承了 Qt 的列表模型，所以 QML 里的 ListView 可以直接把它当作数据源来显示。
每首歌存储的信息包括：标题、艺术家、专辑名、时长、文件路径。这些信息在添加歌曲时由 MetadataReader 自动读取。
添加歌曲时会立即保存到本地，删除歌曲时也一样。保存位置在 `~/.config/QTmusic/QTmusic.conf`，用的是 Qt 自带的 QSettings 机制。所以下次打开程序，之前的播放列表会自动恢复。
红心收藏功能也在这一类里。用了一个集合来记录哪些歌曲被收藏了（按文件路径来判断）。切换收藏状态、查询是否收藏、获取所有收藏歌曲，这些方法都暴露给了 QML。

### 元数据读取器 (MetadataReader)

读取音频文件里的信息（标题、歌手、专辑、时长）。它内部也用了一个播放器，但设置了静音，所以用户不会听到声音。
支持批量读取。如果用户一次选了多首歌，它会排队逐个处理，读完一首发一个信号，QML 收到信号后把歌曲加到播放列表里。

### 歌词搜索器 (LyricsSearcher)

用来搜索本地 `.lrc` 歌词文件。它会在几个目录里搜索，包括当前目录、系统的音乐目录、下载目录等。可以用歌名关键字搜索，也可以列出所有找到的歌词文件。

## QML 界面设计

### 视图切换

主窗口用 StackView 来切换不同的内容区域。通过一个 `currentView` 变量控制当前显示哪个页面：
- currentView = 0：显示播放列表
- currentView = 1：显示歌词
- currentView = 2：显示"我喜欢"收藏列表
侧边栏点击不同选项时，会发出信号告诉主窗口切换视图。

### 界面通信

main.cpp 启动时，把 MusicManager 和 MetadataReader 这两个 C++ 对象注入到 QML 环境中，所以 QML 文件里可以直接用 `musicManager` 和 `metadataReader` 这两个名字来调用它们。
QML 调用 C++ 的方式：直接调用方法，比如 `musicManager.play()`、`musicManager.playlistModel.toggleLike(url)`。
C++ 通知 QML 的方式：发射信号，QML 里用 Connections 监听，比如播放状态变了、歌词更新了、收藏状态变了。

## 数据流

添加歌曲的完整流程：

1. 用户点击菜单"打开音乐文件"，选择音频文件
2. QML 把文件路径传给 MetadataReader
3. MetadataReader 逐个读取文件信息，读完后发出信号（标题、歌手、时长等）
4. QML收到信号，把这些信息添加到 PlaylistModel
5. PlaylistModel 添加后自动保存到本地
6. 如果是第一首歌，自动开始播放
播放音乐时，歌词更新的流程：
1. 启动播放时，MusicManager 查找同目录的 .lrc 文件
2. 解析歌词内容，提取时间戳和文字
3. 播放过程中，每帧更新当前进度
4. 根据进度找到当前应该显示的歌词行
5. 通知 QML 更新歌词界面，高亮当前行

## 如何扩展功能
想添加新页面：
1. 在 `qml/components/` 下新建一个 QML 文件
2. 在 CMakeLists.txt 里注册这个文件
3. 在 Main.qml 的 StackView 里创建一个 Component 包装它
4. 在 Sidebar.qml 里加一个导航按钮
5. 在 Main.qml 的视图切换逻辑里加一个 case
6. 完善视图切换功能
